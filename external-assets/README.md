Store local reference assets here when they should stay out of git.

Current ignore rule:
- Everything under `external-assets/` is ignored.
- This README stays tracked so the directory purpose is documented.
